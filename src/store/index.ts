import { createStore } from 'vuex'

export default createStore({
  // In state, we are keeping our data we are sharing our components
  state: {
  },
  // Getters are like computed property.
  getters: {
  },
  // Simple functions to mutate a state
  mutations: {
  },
  // Actions are like method
  actions: {
    register () {
      return 0
    }
  },
  modules: {
  }
})
